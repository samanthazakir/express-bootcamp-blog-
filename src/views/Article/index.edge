@layout('layouts.rightSidebar')


@section('page-content')
    
    @each(post in posts.toJSON())
        @!component('components.article' , title=post.title , body = post.body , id=post.id, time=post.createdAt)
    @endeach

@endsection